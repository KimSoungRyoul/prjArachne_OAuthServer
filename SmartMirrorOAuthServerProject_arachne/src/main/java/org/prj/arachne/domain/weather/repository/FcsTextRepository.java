package org.prj.arachne.domain.weather.repository;

import org.prj.arachne.domain.weather.FcsText;
import org.springframework.data.jpa.repository.JpaRepository;

public interface FcsTextRepository extends JpaRepository<FcsText, Long>{

}
